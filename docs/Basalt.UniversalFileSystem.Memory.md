# Basalt.UniversalFileSystem.Memory

This package contains implementations for memory filesystem. Usually it should not be used in production. It's created for testing cases.

## Configurations

`Basalt.UniversalFileSystem.Memory` doesn't need extra configurations. All it needs are [common configuration](./Basalt.UniversalFileSystem.md#common-configurations).
